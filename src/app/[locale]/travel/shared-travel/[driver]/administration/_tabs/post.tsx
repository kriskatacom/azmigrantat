"use client";

import { Driver } from "@/lib/services/driver-service";

export default function PostTab({ driver }: { driver: Driver }) {
    return (
        <div>
            <div>Post</div>
        </div>
    );
}
